<phone:PhoneApplicationPage
    x:Class="TheMainEvent_Capstone.Pages.EventPanorama"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait"  Orientation="Portrait"
    shell:SystemTray.IsVisible="False">
	
	<toolkit:TransitionService.NavigationInTransition>
		<toolkit:NavigationInTransition>
			<toolkit:NavigationInTransition.Backward>
				<toolkit:TurnstileFeatherTransition Mode="BackwardIn"/>
			</toolkit:NavigationInTransition.Backward>
			<toolkit:NavigationInTransition.Forward>
				<toolkit:TurnstileFeatherTransition Mode="ForwardIn" BeginTime="0:0:0.7"/>
			</toolkit:NavigationInTransition.Forward>
		</toolkit:NavigationInTransition>
	</toolkit:TransitionService.NavigationInTransition>
	<toolkit:TransitionService.NavigationOutTransition>
		<toolkit:NavigationOutTransition>
			<toolkit:NavigationOutTransition.Backward>
				<toolkit:TurnstileFeatherTransition Mode="BackwardOut"/>
			</toolkit:NavigationOutTransition.Backward>
			<toolkit:NavigationOutTransition.Forward>
				<toolkit:TurnstileFeatherTransition Mode="ForwardOut"/>
			</toolkit:NavigationOutTransition.Forward>
		</toolkit:NavigationOutTransition>
	</toolkit:TransitionService.NavigationOutTransition>

	<phone:PhoneApplicationPage.Resources>
		<Storyboard x:Name="tweetGotFocusStoryboard">
			<DoubleAnimation
            Storyboard.TargetName="tweetBox"
            Storyboard.TargetProperty="Height"
            From="72.0" To="100.0" Duration="0:0:1"
            AutoReverse="True"/>
		</Storyboard>
		<Storyboard x:Name="tweetLostFocusStoryboard">
			<DoubleAnimation
            Storyboard.TargetName="tweetBox"
            Storyboard.TargetProperty="Height"
            From="72.0" To="100.0" Duration="0:0:1"
            AutoReverse="True"/>
		</Storyboard>

	</phone:PhoneApplicationPage.Resources>

	<!--LayoutRoot contains the root grid where all other page content is placed-->
	<Grid x:Name="LayoutRoot">
		<phone:Panorama x:Name="rootPanorama" Title="Event Title" >

			<phone:PanoramaItem>
				<Grid Margin="0,-40,0,0">
					<StackPanel>
						<StackPanel Orientation="Horizontal"  toolkit:TurnstileFeatherEffect.FeatheringIndex="0">
							<TextBlock x:Name="dateBlock" Text="Date" Style="{StaticResource PhoneTextNormalStyle}"/>
							<TextBlock x:Name="timeBlock" Text="Time" Style="{StaticResource PhoneTextNormalStyle}"/>
							<TextBlock x:Name="costBlock" Text="$5.00" Margin="10,0,0,0" Style="{StaticResource PhoneTextNormalStyle}"/>
						</StackPanel>
						<TextBlock x:Name="addressBlock" Text="Location should go here" Style="{StaticResource PhoneTextNormalStyle}"  toolkit:TurnstileFeatherEffect.FeatheringIndex="1"/>
						<TextBlock x:Name="descriptionBlock" HorizontalAlignment="Left" Style="{StaticResource PhoneTextNormalStyle}" Margin="9,0,0,0" TextWrapping="Wrap" Text="Bio Section. This is where the user bio is supposed to go whenever it is implemented. I tsimply need sto be bound to " VerticalAlignment="Top"  toolkit:TurnstileFeatherEffect.FeatheringIndex="2"/>
						<TextBlock x:Name="detailsBlock" HorizontalAlignment="Left" Margin="9,0,0,0" FontSize="18" TextWrapping="Wrap" Text="Details: Here are some details one may need to know" VerticalAlignment="Top"  toolkit:TurnstileFeatherEffect.FeatheringIndex="3"/>
						<TextBlock x:Name="statusBlock" HorizontalAlignment="Left" Margin="9,0,0,0" FontSize="16" TextWrapping="Wrap" VerticalAlignment="Top"  toolkit:TurnstileFeatherEffect.FeatheringIndex="4"/>
						<Button x:Name="attendButton" Content="Attend" Width="135" Click="attendButton_Click" toolkit:TurnstileFeatherEffect.FeatheringIndex="5"/>
					</StackPanel>
				</Grid>
			</phone:PanoramaItem>

			<!--Panorama item two-->
			<phone:PanoramaItem>
				<Grid Margin="0,-40,0,0">
					<StackPanel >
						<StackPanel Orientation="Horizontal">
							<toolkit:PhoneTextBox x:Name="tweetBox" Text="Description" Width="320" AcceptsReturn="True" TextWrapping="Wrap"/>
							<Button Content="Tweet"/>
						</StackPanel>
						<phone:LongListSelector
						x:Name="EventsList" 
						ItemsSource="{Binding Tweets}" 
						SelectionChanged="EventsList_SelectionChanged"
						toolkit:TurnstileFeatherEffect.FeatheringIndex="6"
						toolkit:TiltEffect.IsTiltEnabled="True">
							<phone:LongListSelector.ItemTemplate>
								<DataTemplate>
									<StackPanel>
										<TextBlock Text="{Binding UserName}"
											   TextWrapping="Wrap"
											   Style="{StaticResource PhoneTextLargeStyle}"
											   />
										<TextBlock 
										Text="{Binding Message}"
										TextWrapping="Wrap"
										Style="{StaticResource PhoneTextSubtleStyle}"
										/>
									</StackPanel>
								</DataTemplate>
							</phone:LongListSelector.ItemTemplate>
						</phone:LongListSelector>
					</StackPanel>
				</Grid>
			</phone:PanoramaItem>
			<phone:PanoramaItem Header="Map">
				<Grid Margin="0,-37,0,0">

				</Grid>
			</phone:PanoramaItem>
		</phone:Panorama>
	</Grid>

</phone:PhoneApplicationPage>