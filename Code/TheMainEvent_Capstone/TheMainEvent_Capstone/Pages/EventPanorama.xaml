<phone:PhoneApplicationPage
    x:Class="TheMainEvent_Capstone.Pages.EventPanorama"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait"  Orientation="Portrait"
    shell:SystemTray.IsVisible="False">

    <!--LayoutRoot contains the root grid where all other page content is placed-->
    <Grid x:Name="LayoutRoot">
        <phone:Panorama Title="Event Title">

            <phone:PanoramaItem>
				<Grid Margin="0,-125,0,0">
					<StackPanel>
						<StackPanel Orientation="Horizontal">
							<TextBlock x:Name="dateBlock" Text="Date" Style="{StaticResource PhoneTextNormalStyle}"/>
							<TextBlock x:Name="timeBlock" Text="Time" Style="{StaticResource PhoneTextNormalStyle}"/>
							<TextBlock x:Name="costBlock" Text="$5.00" Margin="10,0,0,0" Style="{StaticResource PhoneTextNormalStyle}"/>
						</StackPanel>
						<TextBlock x:Name="addressBlock" Text="Location should go here" Style="{StaticResource PhoneTextNormalStyle}"/>
						<TextBlock x:Name="descriptionBlock" HorizontalAlignment="Left" Style="{StaticResource PhoneTextNormalStyle}" Margin="9,0,0,0" TextWrapping="Wrap" Text="Bio Section. This is where the user bio is supposed to go whenever it is implemented. I tsimply need sto be bound to " VerticalAlignment="Top" />
						<TextBlock x:Name="detailsBlock" HorizontalAlignment="Left" Margin="9,0,0,0" FontSize="18" TextWrapping="Wrap" Text="Details: Here are some details one may need to know" VerticalAlignment="Top" />
						<TextBlock x:Name="statusBlock" HorizontalAlignment="Left" Margin="9,0,0,0" FontSize="16" TextWrapping="Wrap" VerticalAlignment="Top" />
						<Button x:Name="attendButton" Content="Attend" Width="135" Click="attendButton_Click"/>
					</StackPanel>
				</Grid>
			</phone:PanoramaItem>

            <!--Panorama item two-->
            <phone:PanoramaItem Header="item2">
                <Grid/>
            </phone:PanoramaItem>
        </phone:Panorama>
    </Grid>

</phone:PhoneApplicationPage>